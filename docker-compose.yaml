services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # This persists your sqlite file on your host machine
    environment:
      - DATABASE_URL=sqlite:///./data/ecommerce.db